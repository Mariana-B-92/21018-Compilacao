/* IO mal utilizado */
vazio principal(vazio);

vazio principal(vazio) {
    real s[] = lers();      /* errado: lers() só pode ser inteiro[] */
    escrever("abc");        /* errado: escrever() não aceita strings */
}