/* Inválido: Protótipos devem aparecer antes das funções. */
vazio principal(vazio);

inteiro soma(inteiro a, inteiro b) {
    retornar a + b;
}

vazio principal(vazio) {
    escrever(soma(1, 2));
}